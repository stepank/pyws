Pyws is a project, which purpuse is to help developers to expose some functions of their systems as public API via SOAP protocol and WSDL. The project is very young, but it will grow. Keep waiting. :)